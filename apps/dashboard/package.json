{
  "name": "@agentscope/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly false --noEmit false || echo 'placeholder build (real Vite setup in 6.1)'",
    "dev": "echo 'placeholder dev (real Vite setup in 6.1)'",
    "preview": "echo 'placeholder preview'",
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'placeholder test (real Vitest setup in 6.1)'",
    "test:watch": "echo 'placeholder'",
    "clean": "rm -rf dist .turbo"
  }
}
