{
  "name": "@agentscope/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo 'placeholder dev (real Hono setup in 3.1)'",
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'placeholder test (real api tests in 3.5+)'",
    "test:watch": "echo 'placeholder'",
    "clean": "rm -rf dist .turbo"
  }
}
