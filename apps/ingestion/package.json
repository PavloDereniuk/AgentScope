{
  "name": "@agentscope/ingestion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "start": "node dist/index.js",
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'placeholder test (worker is integration-tested manually)'",
    "test:watch": "echo 'placeholder'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@agentscope/db": "workspace:^",
    "@agentscope/shared": "workspace:^",
    "@grpc/grpc-js": "^1.12.5",
    "@solana/web3.js": "^1.95.5",
    "@triton-one/yellowstone-grpc": "^1.3.0",
    "bs58": "^6.0.0",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "pino-pretty": "^11.3.0",
    "tsx": "^4.19.2"
  }
}
