{
  "name": "@agentscope/detector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@agentscope/db": "workspace:*",
    "@agentscope/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'placeholder test (real detector tests in 5.3+)'",
    "test:watch": "echo 'placeholder'",
    "clean": "rm -rf dist .turbo"
  }
}
