{
  "name": "@agentscope/elizaos-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'placeholder test (real plugin tests in 7.5)'",
    "clean": "rm -rf dist .turbo"
  }
}
